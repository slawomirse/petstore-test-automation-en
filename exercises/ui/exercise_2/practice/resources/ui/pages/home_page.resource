*** Settings ***
# TODO: Import the Browser library
# TODO: Import ../../variables.resource

*** Variables ***
# TODO: Add locator variables for the home page elements:
# ${HOME_PAGE_HERO_TITLE}   - xpath for the hero title element
# ${HOME_PAGE_USER_LABEL}   - xpath for the user badge element (#user-badge)
# ${HOME_PAGE_LOGIN_BUTTON} - xpath for the auth button (#btn-auth)
# ${HOME_PAGE_DEFAULT_PET_CARD} - xpath for the default pet card (#featured-pets > [data-id=DEFAULT_PET_ID])
# ${HOME_PAGE_DEFAULT_PET_NAME} - xpath for the default pet name (h3 inside the card)

*** Keywords ***
# Keywords will be added in later exercises
