*** Settings ***
Resource    resources/common.resource

*** Test Cases ***
Default Pet Should Exist
    # TODO: Use Send GET Request keyword to call /pet/${DEFAULT_PET_ID}
    # TODO: The keyword already checks the status code (default 200)
